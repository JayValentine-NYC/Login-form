const span = document.querySelectorAll(span);
const submitBtn = documtne.getElementById(submit);
const form = document.querySelectorAll(input);

function refresh(e){
form.addEventListener('submit', ()=>{
    e.preventDefault();
})
}